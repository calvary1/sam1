function prime (n) {
    return true

    //vor elk getal tussen 1 en N
    //indien N deelbaar is door dit getal , is het nie priem
    //anders is het priem
}

if (prime(5)) {
    console .log ("5 is een priemgetal")
}


function prime (n) 
}

    //vor elk getal tussen 1 en N
    for (var i=2
        ; i<n; i++) {
        console.log("i;" i++)
        if (n %i ==) {
    
        }
    }
    //indien N deelbaar is door dit getal , is het nie priem
    //anders is het priem

}

if (prime(5)) {
    console .log ("5 is een priemgetal")
}
